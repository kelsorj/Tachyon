<Window x:Class="VStackPlugin.Diagnostics"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3" />
        </Style>
    </Window.Resources>
    
    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <TextBlock VerticalAlignment="Center" Margin="3" Text="Select labware:" />
            <ComboBox VerticalAlignment="Center" ItemsSource="{Binding AvailableLabware}" IsSynchronizedWithCurrentItem="True" Margin="5,0,0,0" />
        </StackPanel>
        <UniformGrid Columns="1" DockPanel.Dock="Bottom">
            <Button Command="{Binding LoadStackCommand}">Load Stack</Button>
            <Button Command="{Binding ReleaseStackCommand}">Release Stack</Button>
            <Button Command="{Binding DownstackPlateCommand}">Downstack Plate</Button>
            <Button Command="{Binding UpstackPlateCommand}">Upstack Plate</Button>
        </UniformGrid>
    </DockPanel>
</Window>

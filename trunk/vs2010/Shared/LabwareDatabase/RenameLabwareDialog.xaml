<Window x:Class="BioNex.Shared.LabwareDatabase.RenameLabwareDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="100" Width="335" Title="Rename Labware" SizeToContent="WidthAndHeight">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3" />
        </Style>
    </Window.Resources>
    
    <Grid Height="59" Width="313">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="91*"></ColumnDefinition>
            <ColumnDefinition Width="238*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="28"></RowDefinition>
        </Grid.RowDefinitions>
        <!-- name -->
        <TextBlock Grid.Row="0" Grid.Column="0">Labware name:</TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding LabwareName}" />
        <!-- ok / cancel -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Right">
            <Button Command="{Binding OkCommand}" Width="60">OK</Button>
            <Button Command="{Binding CancelCommand}" Width="60">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>

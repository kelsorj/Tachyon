<Window x:Class="BioNex.Shared.LabwareDatabase.AddLabwareDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="155" Width="372" Title="Add New Labware">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3" />
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="91*"></ColumnDefinition>
            <ColumnDefinition Width="238*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="28"></RowDefinition>
        </Grid.RowDefinitions>
        <!-- name -->
        <TextBlock Grid.Row="0" Grid.Column="0">Labware name:</TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding LabwareName, UpdateSourceTrigger=PropertyChanged}" />
        <!-- manufacturer -->
        <TextBlock Grid.Row="1" Grid.Column="0">Notes:</TextBlock>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Notes}" />
        <!-- product -->
        <TextBlock Grid.Row="2" Grid.Column="0">Tags:</TextBlock>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Tags}" />
        <!-- ok / cancel -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Right">
            <Button Command="{Binding OkCommand}" Width="60">OK</Button>
            <Button Command="{Binding CancelCommand}" Width="60">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>

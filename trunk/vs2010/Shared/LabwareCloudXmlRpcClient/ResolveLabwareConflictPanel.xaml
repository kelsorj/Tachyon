<Window x:Class="BioNex.Shared.LabwareCloudXmlRpcClient.ResolveLabwareConflict"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Title="Resolve Labware Conflict"
             d:DesignHeight="300" d:DesignWidth="478"
             SizeToContent="WidthAndHeight" >
    
    <StackPanel>
        <Label Content="{Binding ResolutionLabel, Mode=OneTime}" Margin="3"></Label>

        <ListView ItemsSource="{Binding Conflicts}">
            <ListView.View>
                <GridView AllowsColumnReorder="False">
                    <GridViewColumn Header="Name" Width="150" DisplayMemberBinding="{Binding Path=Name}" />
                    <GridViewColumn Header="Local Value" Width="150" DisplayMemberBinding="{Binding Path=LocalValue}" />
                    <GridViewColumn Header="Remote Value" Width="150" DisplayMemberBinding="{Binding Path=RemoteValue}" />
                </GridView>
            </ListView.View>
        </ListView>


        <Button Command="{Binding AcceptCommand}" Margin="3">Accept remote version</Button>
        <Button IsDefault="True" IsCancel="True" Command="{Binding DiscardCommand}" Margin="3">Discard remote and keep local version</Button>
    </StackPanel>
</Window>

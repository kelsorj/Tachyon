<Window x:Class="BioNex.Shared.BioNexGuiControls.UserInputDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="132" Width="336" WindowStartupLocation="CenterScreen" ShowInTaskbar="False"
             FocusManager.FocusedElement="{Binding ElementName=_user_barcode}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>    
        <TextBlock Grid.Row="0" Text="{Binding PromptText}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="3" IsHitTestVisible="False" />
        <TextBox Name="_user_barcode" Grid.Row="1" Text="{Binding UserText, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="3" TabIndex="0" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Command="{Binding OkCommand}" Margin="3" Width="60" IsDefault="True" TabIndex="1">OK</Button>
            <Button Command="{Binding CancelCommand}" Margin="3" Width="60" TabIndex="2">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>

<UserControl x:Class="AmuletTestApp.AmuletToPcUart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="RPC" FontWeight="DemiBold" FontSize="24" Foreground="{Binding AmuletToPcRpcTextColor}" />
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock>Byte 0:</TextBlock>
            <TextBox IsReadOnly="True" Text="{Binding AmuletToPcByte}" />
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock>Word 0:</TextBlock>
            <TextBox IsReadOnly="True" Text="{Binding AmuletToPcWord}" />
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <TextBlock>String 0:</TextBlock>
            <TextBox IsReadOnly="True" Text="{Binding AmuletToPcString}" />
        </StackPanel>
    </Grid>
</UserControl>

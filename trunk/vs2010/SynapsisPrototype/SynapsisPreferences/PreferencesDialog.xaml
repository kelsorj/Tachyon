<Window x:Class="BioNex.SynapsisPrototype.PreferencesDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Title="Synapsis Preferences">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="2" />
            <Setter Property="Width" Value="75" />
        </Style>
    </Window.Resources>
    
    <DockPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Bottom">
            <Button Command="{Binding OkCommand}">OK</Button>
            <Button Command="{Binding CancelCommand}">Cancel</Button>
        </StackPanel>
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!-- low battery threshold -->
            <TextBlock Grid.Row="0" Grid.Column="0">Low battery threshold percentage:</TextBlock>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding LowBatteryThresholdPercentage, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <!-- Light IO device -->
            <TextBlock Grid.Row="1" Grid.Column="0">Light I/O device:</TextBlock>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding LightIODevice, UpdateSourceTrigger=PropertyChanged}"></TextBox>            
            <!-- Password protect diagnostics (currently not used-->
            <!-- Pre-protocol message filename -->
            <TextBlock Grid.Row="2" Grid.Column="0">Pre-protocol message filename:</TextBlock>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding PreProtocolMessageFilename, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <!-- return tipbox -->
            <TextBlock Grid.Row="3" Grid.Column="0">Return tipboxes to original location:</TextBlock>
            <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding ReturnTipBoxToOriginalLocation, UpdateSourceTrigger=PropertyChanged}"></CheckBox>
            <!-- robot disable reset bit -->
            <TextBlock Grid.Row="4" Grid.Column="0">Robot disable reset bit:</TextBlock>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding RobotDisableResetBit, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <!-- run forever -->
            <TextBlock Grid.Row="5" Grid.Column="0">Run forever:</TextBlock>
            <CheckBox Grid.Row="5" Grid.Column="1" IsChecked="{Binding RunForever, UpdateSourceTrigger=PropertyChanged}"></CheckBox>
        </Grid>
    </DockPanel>
</Window>

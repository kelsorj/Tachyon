<Window x:Class="BioNex.SynapsisPrototype.AddNewDevice"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="142" d:DesignWidth="351" Title="Add New Device" x:Name="root"
             Width="350" Height="151" ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
             FocusManager.FocusedElement="{Binding ElementName=_device_name}">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3" />
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="91*"></ColumnDefinition>
            <ColumnDefinition Width="238*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <!-- name -->
        <TextBlock Grid.Row="0" Grid.Column="0">Device name:</TextBlock>
        <TextBox Name="_device_name" Grid.Row="0" Grid.Column="1" Text="{Binding DeviceName}" />
        <!-- manufacturer -->
        <TextBlock Grid.Row="1" Grid.Column="0">Manufacturer:</TextBlock>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Manufacturer}" />
        <!-- product -->
        <TextBlock Grid.Row="2" Grid.Column="0">Product type:</TextBlock>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ProductType}" />
        <!-- ok / cancel -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Right">
            <Button Command="{Binding OkCommand}" Width="60" IsDefault="True">OK</Button>
            <Button Command="{Binding CancelCommand}" Width="60">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>

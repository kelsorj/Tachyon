<UserControl x:Class="BioNex.TipBoxManager.TipBoxLocationControl"
             x:Name="root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <!--:DesignHeight="300" d:DesignWidth="300"-->
    <WrapPanel DataContext="{Binding ElementName=root}" Margin="2">
        <Button Command="{Binding SetNewCommand}">Set New</Button>
        <Button Command="{Binding SetEmptyCommand}">Set Empty</Button>
        <TextBlock Width="50" TextAlignment="Center" VerticalAlignment="Center" Text="{Binding Status, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock VerticalAlignment="Center" Text="{Binding Location}" />
    </WrapPanel>
</UserControl>

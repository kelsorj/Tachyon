<Window x:Class="BioNex.IWorksPlugins.Diagnostics"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Title="Hive Upstacker Device Diagnostics">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Grid.Column="0" Command="{Binding UpstackCommand}">Test Upstack</Button>
        <Button Grid.Column="1" Command="{Binding DownstackCommand}">Test Downstack</Button>
        <GroupBox Grid.Row="1" Grid.ColumnSpan="2" Header="Connection Status" BorderBrush="Orange" DockPanel.Dock="Bottom">
            <Grid>
                <Rectangle Fill="{Binding ConnectionStatusColor}" />
                <TextBlock Text="{Binding ConnectionStatusText}" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16" Foreground="Goldenrod"/>
            </Grid>
        </GroupBox>
    </Grid>
</Window>

## Tachyon Infra (Redis + MinIO)
## Copy to .env if you want to override defaults:
##   cp env.example .env

# MinIO credentials (change for anything beyond local dev)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin





